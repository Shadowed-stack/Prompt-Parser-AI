[project]
name = "prompt-parser-final"
version = "1.0.0"
requires-python = ">=3.10"

dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "python-dotenv",
    "requests",
    "langgraph",
    "sentence-transformers",
    "chromadb",
    "numpy",
    "pandas",
    "streamlit"
]
