# Core
pydantic>=2.6,<3
pydantic-settings>=2.2,<3

# LLM / Workflow
requests>=2.31
langgraph>=0.1.0

# Embeddings + Vector DB
sentence-transformers>=2.7
chromadb>=0.5

# Data loading — required for load_from_parquet() and clean_parquet()
# Needed to load universal_index_final.parquet (254k rows, 25MB)
pandas>=2.0
pyarrow>=14.0

# UI
streamlit>=1.35
